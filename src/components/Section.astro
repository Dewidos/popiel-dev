---
const { title, ...props } = Astro.props;
---

<section class="flex flex-col px-3 py-10" {...props}>
  {title && <h2 class="mb-12 text-center text-4xl font-bold">{title}</h2>}
  <slot />
</section>
